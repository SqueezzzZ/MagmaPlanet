using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class buttonsScript : MonoBehaviour
{
    public GameObject Player;

    private void Start()
    {

    }
    public void onClickButton()
    {
        //float v = Player.GetComponent<playerScript>().maxJumpDist;
        //Player.GetComponent<Rigidbody>().AddForce(Player.GetComponent<playerScript>().axisSpeed,ForceMode.Impulse);
    }
}
